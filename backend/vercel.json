{
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "npm install && npx tsc -p tsconfig.json",
  "routes": [
    { "src": "/(.*)", "dest": "api/index.js" }
  ]
}
