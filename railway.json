{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install --ignore-scripts && npx tsc -p tsconfig.json"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/main.js",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
