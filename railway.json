{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install --ignore-scripts && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/main",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
