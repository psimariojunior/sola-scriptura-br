'use client';

import { SocraticMode } from '@/components/Biblia/SocraticMode';

export default function SocraticoPage() {
  return (
    <div className="h-screen flex flex-col">
      <SocraticMode />
    </div>
  );
}
